"""Handlers sub-package for Cattemis Bot.

Each handler module registers its routes on the shared ``dp`` Dispatcher
imported from ``cattemis_bot.main``.  This package simply documents what is
available; the actual ``include_router`` calls happen in ``main.py``.
"""
